version: "3.9"

services:
  angus-main:
    build: .
    container_name: angus-main
    restart: unless-stopped
    ports:
      - "3001:3001"
    networks:
      - web

networks:
  web:
    external: true